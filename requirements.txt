# BLUE-Gen - Requirements
# Installation: pip install -r requirements.txt

# ── Core dependencies ──────────────────────────────────────────────────────
streamlit>=1.28.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0

# ── Frontend & Visualization ───────────────────────────────────────────────
plotly>=5.14.0  # Interactive charts for signal visualization
plotly-express>=5.14.0

# ── RAG & Text Processing ─────────────────────────────────────────────────
chromadb>=0.4.0
sentence-transformers>=2.2.0
langchain>=0.1.0
langchain-google-genai>=0.1.0
PyPDF2>=3.0.0
transformers>=4.35.0
google-generativeai>=0.5.0

# ── Image Generation & Fine-tuning ────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.38.0  # Stable Diffusion v1.5 pipeline
peft>=0.19.0  # LoRA fine-tuning with get_peft_model support
Pillow>=10.0.0
safetensors>=0.4.0  # Model weights serialization
accelerate>=0.26.0  # GPU/CPU model acceleration

# ── Signal & Forecasting ──────────────────────────────────────────────────
statsmodels>=0.14.0
scikit-learn>=1.3.0
scipy>=1.11.0
prophet>=1.1.0  # Time series forecasting
openpyxl>=3.1.0  # Excel file reading (.xlsx)
pyarrow>=12.0.0  # Arrow support for ChromaDB

# ── Utilities & Networking ────────────────────────────────────────────────
tqdm>=4.66.0
requests>=2.31.0
httpx>=0.24.0
pydantic>=2.0.0
protobuf==3.20.3  # CRITICAL: ChromaDB compatibility (must be <4.0.0)

# ── Development (optional) ────────────────────────────────────────────────
# jupyter>=1.0.0
# black>=23.0.0
# pylint>=3.0.0
# pytest>=7.0.0
